version: "0.1"  # optional since v1.27.0
services:
  web:
    build: .
    ports:
      - "27017:27017"
    volumes:
      - .:/code
      - logvolume01:/var/log
    links:
      - redis
  redis:
    image: redis
volumes:
  logvolume01: {}